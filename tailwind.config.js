/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx}"],  // ← Tells Tailwind where to find your React files
  theme: {
    extend: {},
  },
  plugins: [],
}